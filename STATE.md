# STATE — Keelhouse (handoff 2026-07-09)

Mirror of `~/Documents/Obsidian Vault/ops/handoffs/handoff-2026-07-08-1519-agent-cli-folder-picker.md`. Overwrite on each handoff.

## Goal

Build **Keelhouse** — a native macOS Tauri 2 app that replaces Jason's VS Code workflow with an agent-first cockpit: project/session rail + Codex-like chat/run surface backed by real `claude`/`codex`/`gemini` CLI terminal panes and composer as the main surface, with raw terminal, CodeMirror editor, and browser/web preview as fast switchable/resizable surfaces. Built on Ghostty's terminal engine. Current work: continue v1 after the verified process/workbench hierarchy slices, while preserving VS Code muscle memory for supported workflows. Repo/package slug remains `agent-cli`.

## Done

- **SPIKE-2 (proven):** full terminal loop in a real Tauri window — pty → `libghostty-vt` (real Ghostty VT parsing, in Rust) → grid snapshot over Tauri IPC → Canvas 2D paint; keydown → Ghostty key `Encoder` → pty. Verified by hand: shell renders, typing/arrows/ctrl-c work, `claude`'s fullscreen TUI renders clean.
- **Shortcuts (proven):** real key encoder (arrows, ctrl, Option-as-Meta word-nav, Fn, Shift+Enter, re-encodes on cursor/kitty mode change); Cmd+V paste (clipboard plugin, bracketed-aware); mouse drag selection + highlight; Cmd+C copy from selection; Cmd+K clear (native menu item).
- **Deep research → docs + planning:** `docs/vision-to-reality-2026-07-08.html` (6 areas, 24/25 claims verified). Library choices locked in PRD/ROADMAP/DECISIONS (2026-07-08 entries).
- **v0 folder-picker slice (VERIFIED 2026-07-08):** restructured pty from spawn-in-`setup()` to on-demand `open_workspace(path)` (spawns `$SHELL` in chosen cwd, tears down + respawns previous pane via `ChildKiller`). Added native folder picker (`plugin-dialog`), last-folder persistence (`plugin-store`, `workspace.json`), and a **Terminal → Open Folder… (Cmd+O)** menu item. Verified by hand with first launch, Cmd+O folder switch, and quit/relaunch persistence; `pwd` in the pane matched the selected folder each time.
- **AGENT-LAUNCH (VERIFIED 2026-07-08):** `open_workspace` now accepts a persisted launch profile. Default profile is `claude` via a login shell so nvm-installed CLIs resolve; backend only executes the supplied profile. Verified by hand: app launched `claude` as the direct child of `spike-2`, cwd was `/Users/jasonpoindexter/Documents/GitHub/apps/agent cli`, and Claude's fullscreen TUI rendered in the window.
- **TERMINAL-HARDEN (VERIFIED 2026-07-08):** snapshots now render Ghostty's visible viewport instead of only the active screen; Shift+PageUp/PageDown and mouse wheel scroll through Ghostty scrollback; key/paste snaps back to live output; resize dimensions are clamped before reaching the pty. Verified with a 300-line fast-output burst, Shift+PageUp/Down scrollback screenshots, resize smoke, and copy-selection audit.
- **APPROOT (VERIFIED 2026-07-08):** promoted `spike-2/` to `app/`; renamed npm package to `agent-cli`, Rust package to `agent-cli`, lib crate to `agent_cli_lib`, product/window title to `agent cli` at the time, and identifier to `com.jasonpoindexter.agent-cli`. Product-facing name later became Keelhouse while the repo/package slug stayed `agent-cli`. Verified from `app/`: `cargo test` (6 tests), `cargo build`, `npm run build`, `npm test`, and `npm run tauri dev`. Runtime smoke launched `target/debug/agent-cli`, spawned direct child `claude`, and `lsof` showed the child cwd was `/Users/jasonpoindexter/Documents/GitHub/apps/agent cli`.
- **PROCESS-ENV (VERIFIED 2026-07-08):** `open_workspace` now validates workspace cwd before spawn, preflights the selected launch command through the same login-shell mode used for launch, returns visible frontend errors instead of silently failing, and emits a pane-exit banner when a launched process exits. Verified with Rust tests (10 total), `npm run build`, `npm test`, `cargo build`, real bad-command launch (visible missing-command banner, no child process under `agent-cli`), real immediate-exit launch (`false`, child exited and no child remained), and real Claude launch regression (`claude` direct child cwd matched repo).
- **DATA-STORAGE (VERIFIED 2026-07-08):** `docs/local-state.md` documents the v0/v0.5 Tauri Store path (`~/Library/Application Support/com.jasonpoindexter.agent-cli/workspace.json`), current schema, reversible reset command, and default `launchProfile` repair path. `roadmap.html` rebuilt from `roadmap.json`.
- **APP-SHELL (VERIFIED 2026-07-08):** frontend now has the stable VS Code-shell replacement shape: left file rail surface, main editor surface, and bottom agent terminal panel. Terminal resize now measures the terminal panel instead of the full window, so the pty grid matches the visible pane. Verified with `npm run build`, `npm test`, `cargo test`, `cargo build`, and real `npm run tauri dev` smoke launching `target/debug/agent-cli` with direct child `claude` in cwd `/Users/jasonpoindexter/Documents/GitHub/apps/agent cli`.
- **FILE-RAIL (VERIFIED 2026-07-08):** backend command `list_workspace_tree(path)` lists the selected workspace with `ignore::WalkBuilder`, `.require_git(false)`, app noisy-folder filtering (`node_modules`, `target`, `dist`, `.git`, etc.), and an 8000-entry cap. Frontend renders the left rail with React Arborist, directory expand/collapse, loading/error/empty states, and file activation into the editor surface. Verified with `npm run build`, `npm test`, `cargo test` (11 tests, including `.gitignore`/noisy-dir coverage), `cargo build`, and real `npm run tauri dev` smoke launching `target/debug/agent-cli` with direct child `claude` in cwd `/Users/jasonpoindexter/Documents/GitHub/apps/agent cli`.
- **FILE-WATCHER (VERIFIED 2026-07-08):** backend command `watch_workspace_tree(path)` owns one debounced `notify`/`notify-debouncer-mini` watcher per selected workspace, filters noisy paths before emitting `workspace-tree-changed`, and frontend reloads the rail from `list_workspace_tree`. Verified with `npm run build`, `npm test`, `cargo test` (12 tests, including watcher noisy-path filtering), `cargo build`, and real `npm run tauri dev` visual smoke: creating `aaa-agent-cli-watch-smoke-dir` after launch appeared in the rail; the smoke directory was removed afterward.
- **RECENT-PROJECTS (VERIFIED 2026-07-08):** frontend persists `recentFolders` in the Tauri Store, dedupes/caps recents, prunes missing folders on switch failure, renders a compact rail switcher, and shares the same `openWorkspace()` path as the native picker. The row now has an explicit `Switch` affordance and pointer-down activation so it is easy to hit in the native window. Verified with `npm run build`, `npm test` (2 files, 7 tests), real `npm run tauri dev`, seeded store (`Keelhouse` current, `postures` recent), clicking `Switch`, `workspace.json` persisting `/Users/jasonpoindexter/Documents/postures`, and the rail/tree visibly switching to the postures project.
- **EDITOR (VERIFIED 2026-07-08):** CodeMirror opens files from the rail with reliable pointer-down activation, line numbers, path context, TS/TSX/JS/JSX/MD syntax modes, dirty/saved status, Save button, and Cmd+S/Ctrl+S. Backend `read_text_file`/`write_text_file` guard UTF-8 workspace files against outside-workspace paths and oversize files. Verified with `npm run build`, `npm test`, `cargo test` (14 tests), `npm run tauri dev`, and native UI smoke: opened `/tmp/agent-cli-editor-smoke.jLqmMU/smoke.txt`, edited through CodeMirror, saved with Cmd+S, and confirmed disk contents changed to `after editor smoke 3`.
- **EDITOR-PARITY-UX (VERIFIED 2026-07-08):** editor orientation chrome now includes active tab/title treatment, breadcrumb path bar, language/size/cursor/status readouts, stale active-file warning, rail dirty marker, active-file reveal through React Arborist `scrollTo`, and per-file CodeMirror selection/scroll capture and restore. Repeatable screenshot QA uses `scripts/capture-editor-qa.sh` / `npm run qa:editor` and stores selected, dirty, save-error, missing, no-file, and 1024x640 narrow artifacts in `docs/qa/editor-parity/`. Verified with `npm run build`, `npm test` (3 files, 13 tests), `cargo test`, `scripts/capture-editor-qa.sh`, and `git diff --check`.
- **Scope correction (2026-07-08):** Jason clarified that the app should keep VS Code's shortcuts and supported file/editor/terminal workings, minus bloat/plugins/extensions. Allowed customization is practical settings only: color themes, font/editor/terminal/layout settings, ignored folders, agent commands, and keybinding overrides.
- **Agent integration scope (2026-07-08):** add built-in MCP/API hooks so Claude/Codex-style agents can cooperate with the app: inspect projects/panes/open files, request app-owned actions, open diffs, create/focus panes, and report status. Keep this permissioned and attributed; do not turn it into a plugin/extension platform.
- **Browser scope correction (2026-07-08):** the app should include a lightweight browser/web-preview pane. It is for localhost apps, docs, auth flows, and generated pages; it is not a full general-purpose browser replacement.
- **Multi-project navigation correction (2026-07-08):** multi-project should use a persistent left project/workspace rail, similar in spirit to Codex's sidebar, not only top tabs. The rail should show open projects and running/exited/attention states; each project owns its file rail, editor tabs, browser preview, and agent panes.
- **Iconography/status correction (2026-07-08):** the app needs polished Codex-quality iconography and status badges across the project rail, file explorer, editor, browser, terminal, agent panes, git state, and common actions. Agent activity should show thinking/running-command/editing/waiting/error/exited/complete states and tool/file events, not hidden chain-of-thought.
- **Composer/settings correction (2026-07-08):** keep terminal panes as the trusted source-of-truth agent UI, but add a compact Codex-style composer for sending prompts/instructions to the selected real agent pane or app actions. Settings must handle serious AI setup: Claude/Codex command profiles, provider/API credentials, MCP servers, env vars, model defaults, permission defaults, and health checks.
- **Context-menu/shortcut correction (2026-07-08):** right-click/Control-click menus are required across the project rail, file explorer, editor tabs/text, terminal panes, browser preview, diff/git surfaces, and agent panes. Common commands need VS Code/macOS shortcuts, menu bar discoverability, context-menu access where relevant, disabled states, and confirmation/undo for destructive actions.
- **Settings parity correction (2026-07-08):** use Codex's settings shape only where it fits. Keep General, Appearance, App configuration, Behavior, Keyboard shortcuts, MCP servers, Browser preview, Agent hooks, Connections, Git, Environments, and Worktrees. Drop Profile, Pets, Usage & billing, Archived chats, and Chat Settings. Park Appshots and Computer use until visual context/browser preview/agent hooks justify them. See `docs/settings-parity.md`.
- **Navigation/session parity correction (2026-07-08):** Codex chat rows map to project sessions, not editor tabs or custom chats. A session is a named task/workbench context under a project: editor tabs, browser URL, agent panes, pane labels/status, and transcript references. Keep New session, Search, Projects, nested session rows, active highlights, and Show more; drop Plugins/account chrome; park Scheduled and archive until sessions/transcripts/hooks exist. See `docs/navigation-parity.md`.
- **Integration scope correction (2026-07-08):** local Git is core; GitHub and GitLab are first-class source-hosting integrations; prefer existing `git`/`gh`/`glab` auth and OS-safe credential storage. Bitbucket/Azure DevOps, Linear/Jira, Slack/Discord are adapter-lane or parked until a concrete workflow needs them. No broad plugin marketplace. See `docs/integrations-scope.md`.
- **Chrome polish correction (2026-07-08):** the workbench chrome needs Codex-level finish: dense rails, calm dark surfaces, consistent tokens, one icon family, complete hover/active/focus/disabled/loading/empty/error states, badges, menus, tooltips, toasts, restrained motion, and screenshot-based visual QA. See `docs/chrome-ui-polish.md`.
- **Editor/terminal parity correction (2026-07-08):** editor must be a robust coding surface like the screenshot: line gutter, syntax fidelity, path/breadcrumb context, file tree integration, tabs, dirty markers, find/replace, restored scroll/selection, external-change protection, and visual QA. Terminal must remain robust: real pty/Ghostty path, agent TUI fidelity, scrollback, resize, selection/copy/paste, bracketed paste, keyboard chords, fast-output checks, and pane lifecycle state. See `docs/editor-terminal-parity.md`.
- **Agent activity timeline correction (2026-07-08):** show Codex-style visible agent events like `Edited a file`, plus thinking/planning summaries, ran command, command failed, read/created/deleted/renamed file, opened diff/browser, requested approval, waiting, errored, exited, and complete. Put compact state in pane headers, recent rows in pane activity strips, aggregate badges in project/session rail, and full per-pane/session logs. Never show hidden chain-of-thought. See `docs/agent-activity-timeline.md`.
- **Composer harness research correction (2026-07-08):** bottom Codex-style composer should start as a prompt router to selected real terminal panes, then grow into an app-owned harness with permission mode, goal chip, target pane, model/profile selector, attachments, stop/send state, approval logging, and activity timeline events. Direct API/MCP agent harness is v2-only and optional. See `docs/composer-harness-research.md`.
- **Vanta harness blind-audit correction (2026-07-08):** composer controls need concrete backing contracts before UI polish: add app-owned agent session handles, minimal app-action gates, normalized activity events, and workflow metrics. Borrow Vanta's session/action ideas, not tmux/Docker/self-evolution/runtime scope. See `docs/harness-contract.md` and `docs/blind-audit-vanta-harness-2026-07-08.md`.
- **Agent-first chat/tray correction (VERIFIED 2026-07-09):** Jason clarified that if the app has Codex-like chat, the raw terminal should not be the default main surface. The main screen now defaults to a chat/run view: activity rows, timeline, and composer backed by the real pty. Raw terminal remains available through an explicit Terminal tab/Raw terminal action. CodeMirror and browser preview live in left/right/bottom/hideable trays with draggable splitter rails instead of owning the primary canvas. Verified with `npm run build`, `npm test`, `npm run qa:editor`, selected/narrow screenshot inspection, and `git diff --check`.
- **VS Code chrome correction (VERIFIED 2026-07-09):** Jason clarified that the outer app chrome should borrow useful VS Code/Codex shell structure, not copy 1:1 controls that do not make sense. The shell now has a compact top status/action bar, persistent side drawer, main agent workbench, and bottom status strip. The header shows real Keelhouse state/actions: workspace, session, selected agent/pane, process status, Open Folder, and Reload Preview. Fake traffic lights, fake back/forward, the search-shaped folder picker, and the nonfunctional activity rail were removed. Activity-drawer navigation remains parked until the buttons switch real content. Verified with `npm run build`, `npm test`, `npm run qa:editor`, selected/narrow screenshot inspection, and `git diff --check`.

## In progress

**Next active slice: ACTIVITY-DRAWERS, then SESSION-RESTORE.** PROJECT-RAIL, PROJECT-SESSIONS, BROWSER-PREVIEW, PANE-MANAGER, PANE-NAMES, AGENT-SESSION-HANDLE, AGENT-ACTIVITY, AGENT-ACTIVITY-LOG, APP-ACTIONS-MINIMAL, COMPOSER-HARNESS, PROCESS-LIFECYCLE, AGENT-FIRST-WORKBENCH, and VSCODE-CHROME-SHELL are verified for the first v1 navigation/session/preview/pane/activity/action/harness/lifecycle/layout/chrome layers; next pass should add activity-drawer navigation only if it switches real side-drawer content before restore locks the layout metadata.

**ACTIVE-FILE-SYNC (VERIFIED 2026-07-08):** active-file metadata persists per workspace in `activeFileByWorkspace`, using the backend-returned canonical workspace root from `open_workspace`. On workspace tree load, the frontend restores the saved active file only when that exact file exists in the current tree; stale persisted paths are cleared instead of opening the wrong file. Watcher/tree refresh now reconciles the selected file to the fresh tree node when the same path still exists, while preserving the missing-file warning when it disappears. Verified with `npm run build`, `npm test` (16 tests), `git diff --check`, and a native `npm run tauri dev` smoke that seeded a temporary workspace and captured the restored active file at `docs/qa/active-file-sync/restore.png`.

**EDITOR-VIEW-STATE (VERIFIED 2026-07-08):** per-file editor state now captures selection anchor/head, scrollTop, and focus intent. User-driven file activation focuses CodeMirror after loading; automatic active-file restore does not steal focus. Verified with `npm run build`, `npm test` (16 tests), `git diff --check`, and native `npm run tauri dev` smoke: set `first.ts` to `Ln 5, Col 9`, switched to `second.ts`, switched back to `first.ts`, captured restored cursor at `docs/qa/editor-view-state/restore.png`, then sent ArrowDown and captured `Ln 6, Col 1` at `docs/qa/editor-view-state/focus-restored.png`.

**DIRTY-DRAFT-PROTECTION (VERIFIED 2026-07-08):** dirty editor navigation is guarded for file switches, recent-project switches, and folder-picker switches. A pending navigation modal offers Cancel, Discard, and Save with the dirty file name and data-loss consequence. Save continues only after the real `write_text_file` call succeeds; failed saves keep the draft and pending navigation open with a modal error. Verified with `npm run build`, `npm test` (26 tests covering prompt trigger logic, save failure, save success, discard, no pending navigation, and modal copy), `npm run qa:editor`, visual proof at `docs/qa/dirty-draft-protection/modal.png`, and `git diff --check`.

**SAVE-ERROR-RECOVERY (VERIFIED 2026-07-08):** failed saves keep the edited CodeMirror buffer in place, render a scoped `Save failed` alert with the underlying error, expose Retry and Open externally actions, disable duplicate retry/save while `editorSaving` is true, and clear the error only through the real `write_text_file` success path. Open externally uses the existing Tauri opener plugin and reports opener failures inside the same recovery surface without hiding Retry. Verified with `npm run build`, `npm test` (29 tests including save-error rendering, disabled retry, and opener-failure copy), `npm run qa:editor`, screenshot proof at `docs/qa/editor-parity/save-error.png`, and `git diff --check`.

**EDITOR-RESPONSIVE-QA (VERIFIED 2026-07-08):** `npm run qa:editor` captures selected, dirty, save-error, missing-file, no-file, and 1024x640 narrow workbench states into `docs/qa/editor-parity/`, plus dirty-draft modal proof in `docs/qa/dirty-draft-protection/modal.png`. Visual audit confirmed the narrow and no-file states preserve rail/editor/terminal orientation without overlapping chrome or unusable squeezed editor surfaces.

**EDITOR-FIND-REPLACE (VERIFIED 2026-07-08):** CodeMirror search is now included in every editor instance via `@codemirror/search`, with the default search keymap for Cmd+F, F3, Cmd+G, and Shift variants, a visible Find button in the editor action row, and replace/replace-all support through the editor-native search panel. The search panel is styled for the Keelhouse chrome and captured in `docs/qa/editor-parity/find-replace.png`. Verified with `npm run build`, `npm test` (29 tests), `npm run qa:editor`, and `git diff --check`.

**EDITOR-LANGUAGE-MODES (VERIFIED 2026-07-08):** editor language extensions were moved into `app/src/editorLanguages.ts` and now cover TS, TSX, JSX, JS, Markdown, HTML, CSS, Rust, JSON, TOML, YAML/YML, shell extensions, and common shell dotfiles. `languageLabelForPath` matches the same supported set for the editor status bar. Verified with `npm run build`, `npm test` (31 tests including all required mode mappings), and `git diff --check`.

**SAVE-CONFLICTS (VERIFIED 2026-07-08):** `read_text_file` now returns `modifiedMs`; `write_text_file` receives `expectedModifiedMs` and rejects stale saves before writing if the on-disk modified time changed. The frontend stores the last-seen modified time, passes it on save, keeps the dirty buffer on conflict, and shows a scoped conflict recovery surface with Reload, Overwrite, and Open externally. Verified with `cargo test` (15 tests including stale-write rejection and unchanged external file contents), `npm run build`, `npm test` (32 tests), `npm run qa:editor`, screenshot proof at `docs/qa/editor-parity/save-conflict.png`, and `git diff --check`.

**LARGE-FILE-POLICY (VERIFIED 2026-07-08):** backend text-file guards now reject files over the 2 MB editor cap before full text load, reject invalid UTF-8/binary files with an explicit unsupported text-file error, and reject oversized save buffers before writing to disk. Verified with `cargo test` (18 tests including large read rejection, binary/invalid UTF-8 read rejection, and large save rejection preserving existing file contents), `npm run build`, `npm test` (32 tests), and `git diff --check`.

**FILE-OPS (VERIFIED 2026-07-08):** backend commands create files/folders, rename, delete, duplicate, and reveal selected rail items while validating every operation against the canonical workspace root. The file rail exposes New File/New Folder buttons plus right-click/Control-click actions for New File, New Folder, Rename, Duplicate, Reveal in Finder, and Delete. Delete confirms destructive operations; rename/delete guard dirty active files; renaming a folder containing the active file moves the editor selection to the new path. Verified with `npm run build`, `npm test` (32 tests), `cargo test` (20 tests, including file-op create/rename/duplicate/delete and guard coverage), and `git diff --check`.

**EDITOR-TABS (VERIFIED 2026-07-08):** editor tabbar now shows multiple open files with close controls, dirty markers, active styling, and no-file fallback. File switches preserve in-memory draft buffers instead of discarding them; dirty inactive tabs stay marked; dirty tab close requires confirmation; workspace switches protect dirty tabs; rename/delete file operations retarget or close affected open tabs. Screenshot QA fixture now covers the multi-tab chrome. Verified with `npm run build`, `npm test` (38 tests, including `editorTabs` helper coverage), `npm run qa:editor`, visual inspection of `docs/qa/editor-parity/selected.png` and `docs/qa/editor-parity/narrow.png`, and `git diff --check`.

**AGENT-PROFILES (VERIFIED 2026-07-09):** terminal titlebar now has selectable Codex, Gemini, Claude, and Shell profiles. The titlebar shows the active label, command line, and launch mode; profile changes call the real `open_workspace` path to restart the pane in the current workspace; successful choices persist to `workspace.json`; failed missing-command launches show the backend preflight error and leave the previous saved profile intact. Fresh state defaults to Codex to avoid burning Claude Code usage during testing; local CLI checks found `codex-cli 0.141.0` and `gemini 0.47.0`. `docs/local-state.md` documents the built-in ids and stored shape. Verified with `npm run build`, `npm test`, `cargo test`, `npm run qa:editor`, visual inspection of `docs/qa/editor-parity/selected.png` and `docs/qa/editor-parity/narrow.png`, and `git diff --check`.

**PRODUCT-NAME-README (VERIFIED 2026-07-09):** Keelhouse is locked as the user-facing product name while `agent-cli` remains the repo/package/binary/storage slug until an explicit migration. `README.md` now carries the product overview, run commands, repository map, and documentation map; `docs/README.md` indexes the docs; `docs/product-positioning.md` records the naming decision and refreshed name sanity check; `app/index.html` and Tauri config use Keelhouse for user-facing title/product metadata. Verified with web sanity search, `npm run build`, and `git diff --check`.

**AGENT-COMPOSER (VERIFIED 2026-07-09):** bottom composer now routes normal drafts to the selected real terminal pane via the pty paste path, sends Enter after paste, shows target profile/project/pane context, supports Stop via Ctrl+C, preserves bounded history, keeps failed app-command drafts intact, and handles Enter, Shift+Enter, Escape, Up, and Down predictably. Minimal app commands route `>save`, `>find`, `>open`, and `>clear` without replacing the terminal transcript. Verified with `npm run build`, `npm test` (48 tests), `cargo test` (20 tests), `npm run qa:editor`, selected/narrow screenshot inspection, and `git diff --check`.

**SHORTCUTS (VERIFIED 2026-07-09):** v0.5 shortcut baseline now lives in `app/src/shortcuts.ts` and `docs/shortcuts.md`, covering active workspace/editor/terminal/composer keys plus explicit planned exceptions for quick-open, command palette, and multi-pane shortcuts. Visible tooltips expose Open, Save, Find, Close Tab, and composer Send shortcuts. Native menu entries expose Open Folder, Save, Find, Close Editor Tab, and Terminal Clear with accelerators. Cmd+W closes the active editor tab through existing dirty-tab protection. Verified with `npm run build`, `npm test` (51 tests), `cargo test` (20 tests), `cargo fmt --check`, `npm run qa:editor`, and `git diff --check`.

**TERMINAL-ROBUSTNESS (VERIFIED 2026-07-09):** backend `open_workspace` now returns a pane id, `pane-exit` events include that id, and the frontend ignores stale exits from replaced panes. The terminal titlebar shows profile, command, launch mode, running/exited status, and cwd. `docs/terminal-robustness.md` maps the done criteria to evidence and records real CLI availability (`claude` 2.1.197 and `codex-cli` 0.141.0). Rust tests cover ANSI truecolor cells, alternate-screen entry/restore, resize snapshots, fast-output scrollback/live tail, bracketed paste, and existing terminal keyboard chords. Screenshot QA covers the terminal header/status in selected and narrow workbench states. Verified with `npm run build`, `npm test` (53 tests), `cargo test` (25 tests), `cargo fmt --check`, `npm run qa:editor`, selected/narrow screenshot inspection, and `git diff --check`.

**CONTEXT-MENUS (VERIFIED 2026-07-09):** the app now has one shared context-menu renderer with compact dark chrome, disabled styling, shortcut labels, and danger states. Right-click/Control-click menus exist on the workspace root, project/session rows, file explorer rows, editor tabs, editor text surface, browser preview, terminal pane, and composer/agent controls. Menus reuse existing commands: Open Folder, New File/Folder, Rename, Duplicate, Reveal, Copy Path, Delete, Open/Close Tab, Save, Find, Open Externally, browser Back/Forward/Reload/Open/Copy URL, Copy/Paste/Clear Terminal, Interrupt, Send/Clear Draft, and Stop Selected Pane. Destructive actions keep the existing confirmations and dirty-buffer guards. Diff/git menus are deferred in `docs/context-menus.md` until those surfaces exist. Verified with `npm run build`, `npm test`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**CHROME-POLISH-SYSTEM (VERIFIED 2026-07-09):** `app/src/App.css` now defines a primitive -> semantic -> component token contract for the current workbench chrome. Shared states cover rail/editor/recovery/composer/modal controls, context menus, badges, terminal status pills, fields, overlays, visible focus, disabled, danger, warning, success, and reduced motion. `docs/chrome-polish-system.md` documents the contract and deferred surfaces. `npm run qa:editor` captures `chrome-states.png` to show status pills, disabled/default/primary/danger buttons, context menu disabled styling, and danger alert styling. Icon replacement was shipped later in ICON-SYSTEM. Verified with `npm run build`, `npm test` (53 tests), `npm run qa:editor`, visual inspection of `docs/qa/editor-parity/chrome-states.png` and `docs/qa/editor-parity/narrow.png`, and `git diff --check`.

**ICON-SYSTEM (VERIFIED 2026-07-09):** current v0.5 chrome uses `lucide-react` only through `app/src/icons.tsx`, giving the app one SVG icon contract instead of ad hoc glyphs. File-tree object icons, rail actions, editor action buttons, terminal status pills, composer controls, browser preview, context-menu rows, and the screenshot QA fixture now use `currentColor` icons on a 16px dense grid. Non-universal icon controls keep visible labels, tooltips, or accessible names. `paneStateIconName` and `agentActivityIconName` cover pane and future activity states, including thinking/running/waiting/error/exited/complete. `docs/icon-system.md` records the rules and deferred git/multi-pane surfaces. Verified with `npm run build`, `npm test` (57 tests), `cargo test` (25 tests), `cargo fmt --check`, `npm run qa:editor`, visual inspection of `docs/qa/editor-parity/selected.png`, `context-menu.png`, `chrome-states.png`, and `narrow.png`, and `git diff --check`.

**ACCESSIBILITY-BASICS (VERIFIED 2026-07-09):** current v0.5 chrome has explicit accessible names for rail actions, workspace root, agent profile picker, terminal canvas, composer draft, and browser preview controls. Editor tabs now put `role="tab"`/`aria-selected` on the actual focusable tab buttons. The terminal canvas is tabbable, labelled, and uses the shared focus ring. Context menus focus the first enabled item on open, keep keyboard focus, support Arrow Up/Down navigation, and close with Escape. `docs/accessibility-basics.md` records the implemented baseline and deferred full modal trapping, canvas transcript screen-reader review, and future git/settings/multi-pane surfaces. Verified with `npm run build`, `npm test` (57 tests), `npm run qa:editor`, visual inspection of `docs/qa/editor-parity/selected.png`, `context-menu.png`, and `narrow.png`, and `git diff --check`.

**PROJECT-RAIL (VERIFIED 2026-07-09):** `openProjects` is now persisted in `workspace.json` separately from `recentFolders`. The left rail shows open project rows with active/running/exited/attention states, promotes opened folders into the rail, prunes missing paths from both recents and open projects, and provides switch/reveal/copy/close context-menu actions. Switching projects uses the real `openWorkspaceDirect` path and restores in-session editor tabs, active file, editor buffers, and editor view state through per-project snapshots. `docs/project-rail.md` records the implementation; project session rows, browser preview URL state, and multiple live panes are now implemented, while richer lifecycle state and cross-relaunch session restore remain dedicated roadmap cards. Verified with `npm run build`, `npm test` (59 tests), `npm run qa:editor`, visual inspection of `docs/qa/editor-parity/selected.png`, `context-menu.png`, and `narrow.png`, and `git diff --check`.

**PROJECT-SESSIONS (VERIFIED 2026-07-09):** `projectSessions` and `activeSessionByProject` now persist in `workspace.json`. The project rail shows nested session rows under each project with active/running/exited/attention states, `New session`, switch, rename, copy name, delete with last-session protection, and Show more/Show fewer collapse. Selecting sessions snapshots/restores current implemented editor context: tabs, active file, buffers, and view state. Pane exits/profile failures update the active session status. `docs/project-sessions.md` records the implementation; browser URLs and live pane state are implemented, while pane labels, transcript references, layout restore, and cross-relaunch snapshot restore remain dedicated later cards. Verified with `npm run build`, `npm test` (64 tests), `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**BROWSER-PREVIEW (VERIFIED 2026-07-09):** the workbench now has a docked iframe browser preview pane between editor and terminal. The toolbar supports Back, Forward, Reload, address/local URL input, Open, and Open externally. Preview URLs normalize `localhost:5173`-style input and persist through `browserPreviewByProject` and `browserPreviewBySession`; switching projects/sessions restores the remembered preview URL. Browser preview context menus expose Back, Forward, Reload, Open externally, and Copy URL. `docs/browser-preview.md` records the implementation and defers dev-server auto-detection plus agent-controlled browser actions to dedicated later cards. Verified with `npm run build`, `npm test` (68 tests), `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**PANE-MANAGER (VERIFIED 2026-07-09):** backend `PtyState` now owns multiple real pty/process panes keyed by pane id. `open_workspace` spawns/focuses a pane without killing panes from other projects; `create_pane`, `focus_pane`, and `close_pane` add, focus, and safely kill exactly one selected pane. `grid` events include `paneId`; the frontend caches snapshots by pane and paints only the focused pane. The terminal header exposes a focused pane strip plus New pane profile, New, and Close controls. Project/session state aggregates all panes for the project. `docs/pane-manager.md` records boundaries for pane names, lifecycle restart/kill-all, layout restore, and app-owned handles. Verified with `npm run build`, `npm test` (70 tests), `cargo test` (26 tests including pane focus/close state), `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**PANE-NAMES (VERIFIED 2026-07-09):** terminal panes now have editable task labels shown in the pane strip and composer target. Labels normalize input, fall back to profile/index names when blank, persist in `paneLabelsBySession` by project-session plus pane slot, and can be edited by double-clicking the pane pill or using `Rename Selected Pane` in the terminal context menu. `docs/pane-names.md` records the implementation and boundaries; full pane layout/process restore remains with SESSION-RESTORE and PROCESS-LIFECYCLE. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**AGENT-SESSION-HANDLE (VERIFIED 2026-07-09):** `app/src/agentSessionHandle.ts` now defines stable pane-backed handle descriptors and handle operations with project/session ids, cwd, pane label, profile id/label, process state, approval mode, exit code, created time, activity metadata, `send`, `interrupt`, `readTail`, and `close`. The composer sends prompts through the active handle; Stop and Close route through handle methods; terminal context menu can copy the latest 20 rendered terminal lines through `readTail`. `docs/agent-session-handle.md` and `docs/harness-contract.md` record implementation and boundaries. Verified with `npm run build`, `npm test` (75 tests), `cargo test`, `cargo fmt --check`, `npm run qa:editor`, and `git diff --check`.

**AGENT-ACTIVITY (VERIFIED 2026-07-09):** `app/src/agentActivity.ts` normalizes visible current/recent activity rows scoped by project/session/pane. The selected pane renders an activity strip above the composer with current handle state plus recent rows for prompt sent, app command ran/failed, edited file, created pane, stop sent, copied output, pane closed, complete, and command failed on non-zero exit. Rows use shared icons/status colors and accessible labels; hidden reasoning is never shown. `docs/agent-activity.md` records boundaries: durable history belongs to AGENT-ACTIVITY-LOG and approval rows become real with APP-ACTIONS-MINIMAL. Verified with `npm run build`, `npm test` (78 tests), `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**AGENT-ACTIVITY-LOG (VERIFIED 2026-07-09):** `agentActivityEvents` now persists up to 200 user-safe events with project/session/pane attribution, kind, label, detail, status, timestamp, target, exit-code, output-reference, and undo-hint fields. The selected pane renders a durable timeline below the activity strip with filters for all supported event kinds. Startup normalization drops malformed stored rows. Current producers cover prompts, app commands, file saves, pane lifecycle actions, copied output, and process exits; git/approval/browser/diff producers use the reserved event fields when those real surfaces land. `docs/agent-activity-log.md`, `docs/agent-activity-timeline.md`, and `docs/local-state.md` record the scope and schema. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**APP-ACTIONS-MINIMAL (VERIFIED 2026-07-09):** `app/src/appActions.ts` defines app-owned action descriptors with kind, target, risk class, requester, reason, and undo hint, then resolves each action to an audit event with approval mode, decision, prompt state, timestamp, and reason. Focus pane, open file, open browser preview, attach reference, interrupt process, create pane, close pane, and composer app commands route through the gate. Prompted, denied, blocked, and composer-originated decisions log approval audit rows to the activity timeline. Open diff remains typed but blocked until the real diff surface lands. `docs/app-actions-minimal.md`, `docs/harness-contract.md`, and `docs/composer-harness-research.md` record the boundary. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**COMPOSER-HARNESS (VERIFIED 2026-07-09):** `app/src/composerHarness.ts` defines per-session composer harness state for permission mode, goal, selected profile, and attachment references. `composerHarnessBySession` persists that state by project/session. The bottom composer shows target project/session/pane, Ask/Approve safe/Full access, profile selector, goal, local file/current file/browser preview attachments with remove controls, and send/stop state. Permission mode drives the app-owned action gate. Prompt sends include transparent goal/attachment context when present, and permission, goal, profile, attachment, prompt, and app-action decisions log to the activity timeline. `docs/composer-harness.md`, `docs/composer-harness-research.md`, `docs/harness-contract.md`, and `docs/local-state.md` record scope and schema. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**PROCESS-LIFECYCLE (VERIFIED 2026-07-09):** backend `terminate_pane` kills a pane's child process while keeping the pane and transcript present for the eventual exit state; backend `restart_pane` spawns a fresh process, focuses it, and removes/kills the replaced process. The frontend preserves pane slot and label across restart, updates project/session status, clears stale snapshots, and logs `Terminate sent`/`Restarted process` activity events. The terminal header shows profile command, launch mode, cwd, state, Restart, Kill, New, and Close; narrow widths collapse lifecycle buttons to icons to avoid overlap. Restart and terminate route through the app action gate with audit events. `docs/process-lifecycle.md`, `docs/app-actions-minimal.md`, `docs/harness-contract.md`, and `docs/pane-manager.md` record boundaries. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.

**AGENT-FIRST-WORKBENCH (VERIFIED 2026-07-09):** the workbench hierarchy now matches the actual workflow: the agent panel defaults to Chat, showing activity strip/log and composer as the primary surface. The raw terminal canvas stays mounted for the real pty but is visible only through the Terminal tab or Raw terminal action. Editor and browser preview are trays that can move left, right, bottom, or hide. Draggable splitter rails resize the whole tray and the editor/browser split; layout mode, sizes, and agent surface mode persist in localStorage. Tray controls live in the agent header so tools can be hidden without losing recovery. Verified with `npm run build`, `npm test`, `npm run qa:editor`, selected/narrow screenshot inspection, and `git diff --check`.

## Next (ordered)

1. **ACTIVITY-DRAWERS:** add activity rail/buttons only when they switch real side-drawer content and support pinned/resizable drawer state.
2. **SESSION-RESTORE:** restore project rail, editor tabs, rail state, and pane layout.
3. **GIT-STATUS:** dirty/new/deleted markers in the file rail.
4. **DIFF-VIEW:** inspect agent-created changes without VS Code.

## Gotchas

- **Zig pinned to 0.15.2.** Every build/run MUST prefix PATH: `PATH="/opt/homebrew/opt/zig@0.15/bin:$PATH"`. Homebrew's default zig 0.16 breaks `libghostty-vt-sys`. Build: `cd app/src-tauri && PATH=... cargo build`. Run: `cd app && PATH=... npm run tauri dev`.
- **`libghostty-vt` types are `!Send`/`!Sync`.** Per pane, the `Terminal` + key `Encoder` are created inside and never leave ONE "terminal thread". A reader thread forwards pty bytes over an `mpsc` channel; only the `Send` pieces (channel `tx`, pty writer, `ChildKiller`) cross threads. Do NOT move `Terminal`/`Encoder` across threads — it won't compile and isn't safe.
- **Data flow:** pty bytes → `Terminal::vt_write` → `snapshot()` (full grid, cells w/ fg/bg/bold) → Tauri event `"grid"` → Canvas 2D. Input: JS `send_key`/`paste`/`resize_pty` commands → current pane's channel → terminal thread encodes + writes. Full-grid snapshots per frame (rAF-coalesced, not dirty-diffed yet — fine until fast output stutters).
- **Key files:**
  - `app/src-tauri/src/lib.rs` — backend: `spawn_pane()`, `open_workspace()`, `handle_key()`, `handle_paste()`, `snapshot()`, `key_from_code()`, `PtyState{pane: Mutex<Option<Pane>>}`, menu, tests.
  - `app/src-tauri/src/lib.rs` — file rail/watch commands: `list_workspace_tree()`, `watch_workspace_tree()`, `FileTreeBuilder`, `is_noisy_dir()`, `watch_event_is_relevant()`.
  - `app/src/App.tsx` — app shell layout, VS Code-style title/activity/status chrome, recent-project switcher, file rail rendering, canvas render loop, input encoding, workspace init (`openWorkspace`/`pickWorkspace`), selection wiring.
  - `app/src/workspaceState.ts` — recent-project normalization, promotion, pruning, and missing-workspace error detection.
  - `app/src/selection.ts` — mouse selection math (`pointFromMouse`, `isCellSelected`, `selectionToText`).
  - `spike-ghostty-vt/` — original parsing-only spike + Zig-pin notes.
- **Planning = source of truth (read before deciding anything):** `PRD.md`, `ROADMAP.md` (has "Execution discipline" + per-phase stack), `DECISIONS.md` (append-only, don't edit past entries), `docs/vision-to-reality-2026-07-08.html`.
- **Execution discipline (locked in ROADMAP):** one thin vertical slice at a time; app runnable at every commit; measure-don't-preempt (no WebGL/SQL/worktrees/theming until measured need); the coding agent drives `lib.rs`/`App.tsx` (Jason reviews, doesn't hand-edit in parallel).
- **Product boundary:** preserve VS Code muscle memory for supported workflows, including shortcuts, menu bar commands, right-click/Control-click context menus, and command-palette access, but do not build plugins/extensions. Settings copy Codex's useful navigation/search/icon structure but keep only concrete knobs: color themes, fonts, keybindings, ignored folders, layout, agent commands, AI connections, MCP servers, browser preview, hooks, Git, GitHub/GitLab/source-control connections, worktrees, model/provider defaults, env vars, credentials, and permission policy. Browser support means a dev preview pane, not a full browser. Multi-project navigation means a left project/workspace rail with per-project sessions/state, not only top tabs. Chrome polish means coherent workbench surfaces/states/tokens/icons, not decorative marketing chrome. The chat/run activity and composer surface is primary; raw terminal is an explicit switchable view; the editor and browser are robust first-class movable/resizable trays, not the default main screen. Agent status shows Codex-style activity/progress rows and tool/file/git events, never hidden chain-of-thought. Composer is a Codex-style router/harness around real pane/session handles and app-owned actions, not a replacement for real terminal agent UIs.
- **Agent hook boundary:** built-in MCP/API is allowed for app-owned commands. Mutating actions need visible approval/undo where appropriate, and every agent-originated action should be attributable.
- **Stack:** Tauri 2 + React/TS/Vite + Rust; `libghostty-vt` 0.2.0; `portable-pty` 0.9; plugins: opener, clipboard-manager, dialog, store. Capabilities in `app/src-tauri/capabilities/default.json`.

## Continuation prompt (for Codex)

Continue Keelhouse: read `STATE.md`, `README.md`, `PRD.md`, `ROADMAP.md`, `DECISIONS.md`, `docs/README.md`, and the linked docs for the next card, then resume at "Next" step 1 — ACTIVITY-DRAWERS. Verified v0/v0.5/v1 slices now include APP-SHELL, FILE-RAIL, FILE-WATCHER, RECENT-PROJECTS, EDITOR, EDITOR-PARITY-UX, ACTIVE-FILE-SYNC, EDITOR-VIEW-STATE, DIRTY-DRAFT-PROTECTION, SAVE-ERROR-RECOVERY, EDITOR-RESPONSIVE-QA, EDITOR-FIND-REPLACE, EDITOR-LANGUAGE-MODES, SAVE-CONFLICTS, LARGE-FILE-POLICY, FILE-OPS, EDITOR-TABS, AGENT-PROFILES, PRODUCT-NAME-README, AGENT-COMPOSER, SHORTCUTS, TERMINAL-ROBUSTNESS, CONTEXT-MENUS, CHROME-POLISH-SYSTEM, ICON-SYSTEM, ACCESSIBILITY-BASICS, PROJECT-RAIL, PROJECT-SESSIONS, BROWSER-PREVIEW, PANE-MANAGER, PANE-NAMES, AGENT-SESSION-HANDLE, AGENT-ACTIVITY, AGENT-ACTIVITY-LOG, APP-ACTIONS-MINIMAL, COMPOSER-HARNESS, PROCESS-LIFECYCLE, AGENT-FIRST-WORKBENCH, and VSCODE-CHROME-SHELL. Product name is Keelhouse; repo/package/binary/storage slug remains `agent-cli` until explicit migration. Keep VS Code muscle memory for supported explorer/editor/browser/terminal flows, keep a real title/sidebar/main/status shell, add activity rail only when it controls real drawers, keep the agent terminal/composer/activity surface as the primary screen, and keep editor/browser as movable/resizable trays; do not add plugin/extension bloat. All builds/runs need `PATH="/opt/homebrew/opt/zig@0.15/bin:$PATH"` (zig 0.15.2 pin). Follow ROADMAP execution discipline: one thin vertical slice at a time, app runnable every commit, measure-don't-preempt.
