# ROADMAP — Keelhouse

Source of truth: `roadmap.json` → `roadmap.html` (rockmap board). Rebuild after editing:

```bash
node rockmap/build-roadmap.mjs roadmap.json roadmap.html
```

Sequential where risk gates the next step; parallel only when cards are independent. Keelhouse replaces Jason's VS Code workflow with an agent-first workbench: Codex-style project chats and composer are primary; raw CLI terminals, files, editor, Git, and browser preview are optional resizable surfaces around them.

Remaining cards are ordered by efficient build dependency: finish the accepted shell chrome, fix control/tray mechanics, pass a full chrome coherence gate, wire local-preview detection, add command/search leverage, prove the app can replace the VS Code habit, then add settings, agent hooks, integrations, distribution, and speculative expansion.

Current build order uses a Value-vs-Effort pass plus dependency gates: ship high-risk basics before more features, keep quick wins only when they unlock the next slice, and park personalization until the core cockpit works.

## Execution discipline (governs every phase)

Added 2026-07-08. The roadmap is now detailed enough to invite building breadth before a slice is real — resist that. The failure mode is over-engineering ahead of proven need.

- **One thin vertical slice at a time.** Each card ships end-to-end and stays usable before the next starts. No half-built breadth.
- **App runnable at every commit.** `app/` must launch and work after each change — never a "big rewrite in flight" state.
- **Measure, don't preempt.** The two open risks get fixed only when they actually bite: fast-output stutter → dirty-region IPC deltas (not WebGL first); 4–8 concurrent agents → measure the process ceiling then. Same for SQL, worktrees, WebGL, theming — build at the phase that needs them, not earlier.
- **One driver per file per slice.** Decided 2026-07-08: **the coding agent drives the code** (`app/src-tauri/src/lib.rs`, `app/src/App.tsx`); Jason reviews rather than hand-editing in parallel, to avoid concurrent-edit clobbers.
- **Rule of 3 / no platform-thinking** (from CLAUDE.md) still apply: no abstraction until 3 concrete uses; no multi-tenancy/plugins/extension system. Settings are allowed only for concrete product knobs: theme, font, keybindings, ignored folders, layout, and agent commands.
- **VS Code muscle memory is a feature.** For supported files/editor/terminal/project workflows, default shortcuts and interactions should match VS Code unless the native terminal path requires a deliberate exception.
- **Commands need multiple doors.** Important actions should be reachable by menu bar/shortcut and either context menu or command palette. Right-click/Control-click is first-class on macOS; no command should exist only as a hidden context-menu item.

## Done so far (research + verification — see DECISIONS.md)

- ~~OSS landscape survey~~ · ~~zellij trial (parked — wrong interaction model)~~ · ~~Super Engineering/cmux evaluated~~ · ~~Hashmark extraction audit~~ · ~~editor-fidelity spike (PASS)~~ · ~~cmux fork-decision audit~~
  - 2026-07-13 Super Engineering live/package audit: borrow quiet persistent chrome, a dedicated searchable settings workspace, explicit scope, provider profiles, configurable palette sources, worktree policy, and setup/run/pre-cleanup/teardown/post-cleanup slots. Keelhouse's real ownership levels are Global, Project, and Chat; do not invent Super's workspace-group object. Keep Keelhouse's chat-first center, dark palette, resizable trays, and accessible DOM; reject the pale home, rounded onboarding cards, and worktree-first object model. See `docs/super-engineering-chrome-audit.md`.
- ~~**libghostty-vt spike — PASSED 2026-07-07.**~~ Real pty → real Ghostty parsing → correct cell readback (`spike-ghostty-vt/`). Proves the terminal *engine* choice.
- ~~**SPIKE-2: render + input loop — PASSED 2026-07-08.**~~ Full loop in a real Tauri window, now promoted into `app/`: pty → libghostty-vt → grid snapshot over IPC → canvas paint; keydown → bytes → pty. Verified: zsh prompt renders, typing echoes, arrows/ctrl-c work, Cmd+V paste works, Cmd+K clears, Option word-nav/delete works, canvas drag selection highlights text, Cmd+C copies selected terminal text. **The rock is dead** — no unproven architecture remains.
- ~~**REFERENCE-PRODUCT-AUDIT — PASSED 2026-07-09.**~~ Zed is the closest chrome-quality reference, not a layout to copy; Keelhouse must translate that level of craft into an agent-first cockpit. From `awesome-agent-orchestrators`, ConstellAgent, herdr, parallel-code, and Clave are the closest targeted references. Orbit is useful for agent workflow mechanics, not shell chrome. See `docs/reference-products.md`.

## v0 — One project, one real agent pane

The whole app in one vertical slice. If this works, everything else is composition; if it doesn't, nothing else matters.

1. ~~**SPIKE-2: Render + input loop** *(the rock)*.~~ **DONE** — proven in the original spike and promoted into `app/` (see above). The render+input pipeline is now a working Tauri app, with copy/selection and core shortcut behavior verified.
2. ~~**SCAFFOLD: Tauri 2 app**.~~ **DONE** — merged into SPIKE-2 (deliberate: the render risk lives at the IPC seam, so proving it required the real scaffold). `app/` is a working Tauri 2 + React/Vite + Rust app with IPC wired.
3. ~~**OPEN-FOLDER:** native folder picker creates the current workspace.~~ **DONE 2026-07-08** — verified first launch, Cmd+O switch, and `pwd` in the pane.
4. ~~**AGENT-LAUNCH:** launch real `claude` in that selected cwd.~~ **DONE 2026-07-08** — persisted launch profile defaults to `claude`; verified child process cwd and rendered TUI.
5. ~~**LAST-WORKSPACE:** quit/relaunch reopens the last folder.~~ **DONE 2026-07-08 for the current shell-pane default** — recheck once AGENT-LAUNCH changes the default command.
6. ~~**TERMINAL-HARDEN:** resize, scrollback basics, fast-output smoke, and keybinding/copy-selection audit.~~ **DONE 2026-07-08** — Ghostty viewport scrollback, Shift+PageUp/PageDown, wheel scroll, resize clamp, 300-line fast-output smoke, and copy-selection audit verified.
7. ~~**APPROOT:** promote `spike-2/` into the real app root/package so daily development starts from the thing that works.~~ **DONE 2026-07-08** — `app/` is the real package/root; npm package/Rust crate/Tauri product identifier were renamed; builds/tests pass; `npm run tauri dev` launches `target/debug/agent-cli` and spawns `claude` in the selected cwd.
8. ~~**PROCESS-ENV:** launch env/PATH/auth checks for Claude/Codex.~~ **DONE 2026-07-08** — bad cwd/command are preflighted before replacing the pane; missing-command errors render visibly; process exits emit a banner for runtime/auth-style failures; Claude launch regression still spawns in the selected cwd.
9. ~~**DATA-STORAGE:** documented local state location and reset path.~~ **DONE 2026-07-08** — `docs/local-state.md` names the Tauri Store path, current `workspace.json` schema, reversible reset command, and default profile repair path.

**v0 done:** open the app, pick a folder, run `claude` in the pane, type and see output — feels like a real terminal. Relaunch returns to the same project.

## v0.5 — The VS Code shell replacement shape

This is where the clarified product point lands: not "terminal app with optional editor later," but the lean subset of VS Code Jason actually uses.

- ~~**APP-SHELL:** stable three-part layout: file rail, editor area, terminal pane area.~~ **DONE 2026-07-08** — React shell now has a left file rail surface, main editor surface, and bottom terminal panel; terminal resize now uses the measured terminal panel instead of the whole window. Verified with `npm run build`, `npm test`, `cargo test`, `cargo build`, and real `npm run tauri dev` smoke launching `target/debug/agent-cli` with direct child `claude` in the repo cwd.
- ~~**FILE-RAIL:** dense project file explorer, with noisy folders ignored.~~ **DONE 2026-07-08** — backend `list_workspace_tree` uses `ignore::WalkBuilder` with gitignore/app noisy-folder filtering; frontend renders a dense React Arborist tree in the left rail, supports directory expand/collapse, and activates files into the editor surface. Verified with `npm run build`, `npm test`, `cargo test` (11 tests, including noisy-dir/gitignore coverage), `cargo build`, and real `npm run tauri dev` smoke launching Claude in the selected cwd.
- ~~**FILE-WATCHER:** live rail updates, gitignore/app ignores, noisy-folder protection.~~ **DONE 2026-07-08** — backend owns a debounced `notify` watcher per selected workspace, filters noisy paths before emitting `workspace-tree-changed`, and frontend refreshes the existing tree source on that event. Verified with `npm run build`, `npm test`, `cargo test` (12 tests, including watcher noisy-path filtering), `cargo build`, and real `npm run tauri dev` visual smoke: creating `aaa-agent-cli-watch-smoke-dir` after launch appeared in the rail, then was removed.
- ~~**RECENT-PROJECTS:** reopen active folders without a picker ceremony.~~ **DONE 2026-07-08** — persisted `recentFolders`, added a compact rail switcher with explicit `Switch` affordance, dedupe/cap/prune helpers, and missing-path cleanup. Verified with `npm run build`, `npm test`, real `npm run tauri dev`, seeded `Keelhouse` + `postures` recents, clicking `Switch`, `workspace.json` persisting `/Users/jasonpoindexter/Documents/postures`, and the rail/tree switching to the postures project.
- ~~**EDITOR:** robust CodeMirror editor that opens files from the rail, edits, saves, and feels like a real coding surface.~~ **DONE 2026-07-08** — CodeMirror editor opens files from the rail, shows line numbers/path context, tracks dirty/saved state, supports Save/Cmd+S, and writes UTF-8 workspace files through guarded Tauri commands. Verified with build/tests plus native UI smoke opening/editing/saving `smoke.txt` and confirming disk contents changed.
- ~~**EDITOR-PARITY-UX:** line gutter, path/breadcrumb context, active-file orientation, dirty markers, view-state restore, and screenshot QA.~~ **DONE 2026-07-08** — editor now has breadcrumb path context, stable gutter styling, active tab/path/status orientation, rail dirty marker, active-file reveal hook, per-file selection/scroll restore, and repeatable screenshot QA artifacts in `docs/qa/editor-parity/`.
- ~~**ACTIVE-FILE-SYNC:** selected file, editor title/path, rail highlight, watcher refresh, and workspace restore stay synchronized.~~ **DONE 2026-07-08** — active file metadata persists per canonical workspace root, relaunch restores the saved file into the rail/editor, stale paths are cleared, and refreshed file trees reconcile the selected file to the fresh node while preserving a missing-file warning. Verified with `npm run build`, `npm test` (16 tests), `git diff --check`, and native `npm run tauri dev` smoke captured at `docs/qa/active-file-sync/restore.png`.
- ~~**EDITOR-VIEW-STATE:** restore cursor, selection, scroll position, and focus per file during the session.~~ **DONE 2026-07-08** — editor view state now stores selection, cursor, scroll, and focus intent per file. User-driven file switches restore focus into CodeMirror, while automatic workspace active-file restore does not steal focus. Verified with `npm run build`, `npm test` (16 tests), native switch-away/switch-back smoke, and screenshots at `docs/qa/editor-view-state/restore.png` and `docs/qa/editor-view-state/focus-restored.png`.
- ~~**DIRTY-DRAFT-PROTECTION:** preserve unsaved edits across file/project switches and require save/discard/cancel before data loss.~~ **DONE 2026-07-08** — dirty file/workspace replacement now opens a Save/Discard/Cancel modal with the file name and consequence; Save only continues after `write_text_file` succeeds; failed saves keep the draft and pending navigation. Verified with `npm run build`, `npm test` (26 tests, including save-failure/discard/save-success/no-pending flows and modal copy), `npm run qa:editor`, visual proof at `docs/qa/dirty-draft-protection/modal.png`, and `git diff --check`.
- ~~**SAVE-ERROR-RECOVERY:** failed saves keep the buffer, show a scoped error, and provide explicit Retry/Open externally recovery actions.~~ **DONE 2026-07-08** — save failures keep the edited buffer, show a scoped `Save failed` alert with the human error, expose Retry and Open externally, disable duplicate retry/save while a write is running, and clear the error only when the real write path succeeds. Verified with `npm run build`, `npm test` (29 tests, including save-error component states), `npm run qa:editor`, refreshed `docs/qa/editor-parity/save-error.png`, and `git diff --check`.
- ~~**EDITOR-RESPONSIVE-QA:** screenshot QA for selected, dirty, save-error, and narrow-width editor states.~~ **DONE 2026-07-08** — repeatable Playwright screenshot QA keeps selected, dirty, save-error, missing-file, no-file, and 1024x640 narrow workbench captures in `docs/qa/editor-parity/`. Visual audit confirmed rail/editor/terminal orientation and no overlapping chrome in the narrow and no-file states. Verified with `npm run qa:editor`.
- ~~**EDITOR-FIND-REPLACE:** local find/replace inside files.~~ **DONE 2026-07-08** — CodeMirror search is now a first-class editor extension with Cmd+F/F3/Cmd+G search keybindings, a visible Find button, and replace/replace-all support through the editor-native search panel. Verified with `npm run build`, `npm test` (29 tests), `npm run qa:editor`, visual proof at `docs/qa/editor-parity/find-replace.png`, and `git diff --check`.
- ~~**EDITOR-LANGUAGE-MODES:** first-class TS/TSX/JSX/JS/MD/HTML/CSS/Rust/JSON/TOML/YAML/shell highlighting.~~ **DONE 2026-07-08** — editor language extensions now cover TypeScript, TSX/JSX, JavaScript, Markdown, HTML, CSS, Rust, JSON, TOML, YAML, and shell scripts/dotfiles. Verified with `npm run build`, `npm test` (31 tests, including mode mapping coverage), and `git diff --check`.
- ~~**SAVE-CONFLICTS:** detect external file edits before overwrite.~~ **DONE 2026-07-08** — reads now return file `modifiedMs`; saves pass the last-seen value and the Rust backend rejects stale writes before overwriting. Conflict recovery shows Reload, Overwrite, and Open externally. Verified with `cargo test` (15 tests, including stale-write rejection), `npm run build`, `npm test` (32 tests), `npm run qa:editor`, visual proof at `docs/qa/editor-parity/save-conflict.png`, and `git diff --check`.
- ~~**LARGE-FILE-POLICY:** safe handling for large or binary files.~~ **DONE 2026-07-08** — backend rejects files over the 2 MB editor cap before full text load, rejects invalid UTF-8/binary files explicitly, and rejects oversized save buffers without touching disk. Verified with `cargo test` (18 tests including large read, binary read, and large save rejection), `npm run build`, `npm test` (32 tests), and `git diff --check`.
- ~~**FILE-OPS:** create/rename/delete/duplicate/reveal from rail.~~ **DONE 2026-07-08** — backend workspace file operations are rooted, guarded, and tested; the rail exposes New File, New Folder, Rename, Duplicate, Reveal in Finder, and Delete through top-level/context-menu paths with destructive confirmations and dirty-buffer protection. Verified with `npm run build`, `npm test` (32 tests), `cargo test` (20 tests, including file-op guard/roundtrip coverage), and `git diff --check`.
- ~~**EDITOR-TABS:** several open files, dirty indicators, close protection.~~ **DONE 2026-07-08** — editor tabs now list multiple open files, preserve in-memory draft buffers while switching, show dirty markers in tabs and the rail, confirm before closing dirty tabs, and keep tabs in sync with rename/delete file operations. Verified with `npm run build`, `npm test` (38 tests, including tab helper coverage), `npm run qa:editor`, visual inspection of selected/narrow captures, and `git diff --check`.
- ~~**AGENT-PROFILES:** Codex default, plus Gemini/Claude/shell profiles without hardcoding one CLI forever.~~ **DONE 2026-07-13** — structured chat defaults to Codex while an explicitly opened raw terminal defaults to blank Shell. Terminal profile changes remain explicit, persist separately in `terminalLaunchProfile`, and preserve missing-command errors without silently switching providers. Verified with frontend helper/coverage tests and packaged shell process inspection.
- ~~**PRODUCT-NAME-README:** real product name and README/docs setup.~~ **DONE 2026-07-09** — Keelhouse is locked as the user-facing product name while `agent-cli` remains the repo/package/storage slug until an explicit migration. Root README now has the product positioning, local commands, repository map, and documentation map; `docs/README.md` is the docs index. App HTML title and Tauri product/window title use Keelhouse. Web sanity check found no obvious active developer-tool/AI-terminal product named Keelhouse; this is not legal clearance.
- ~~**AGENT-COMPOSER:** Codex-style bottom input that routes prompts/instructions to the selected real terminal agent or app action.~~ **DONE 2026-07-09** — bottom composer routes normal drafts through the real pty paste path, sends Enter to the active pane, exposes selected profile/project/pane context, supports Stop via Ctrl+C, preserves bounded draft history, handles Enter/Shift+Enter/Escape, and routes minimal app commands (`>save`, `>find`, `>open`, `>clear`) without replacing the terminal transcript. Verified with `npm run build`, `npm test` (48 tests), `cargo test` (20 tests), `npm run qa:editor`, visual inspection of selected/narrow captures, and `git diff --check`.
- ~~**SHORTCUTS:** VS Code-compatible shortcut map across terminal/editor/chrome.~~ **DONE 2026-07-09** — `docs/shortcuts.md` and `app/src/shortcuts.ts` now define the v0.5 active shortcut baseline plus planned exceptions for quick-open, command palette, and multi-pane keys. Tooltips expose Open, Save, Find, Close Tab, and composer Send shortcuts; native menu entries cover Open Folder, Save, Find, Close Editor Tab, and Terminal Clear. Cmd+W closes the active editor tab with dirty protection. Verified with `npm run build`, `npm test` (51 tests), `cargo test` (20 tests), `cargo fmt --check`, `npm run qa:editor`, and `git diff --check`.
- ~~**TERMINAL-ROBUSTNESS:** single-pane terminal fidelity for agent TUIs, alternate screen, ANSI/truecolor, scrollback, paste, selection, resize, keyboard chords, pane status, and fast output.~~ **DONE 2026-07-09** — pane launches now carry a pane id, and exit events are ignored unless they match the current pane, preventing stale killed panes from overwriting a newer running pane. The terminal header shows profile, command, launch mode, running/exited state, and cwd. Rust tests cover truecolor cells, alternate-screen entry/restore, resize dimensions, fast-output scrollback/live tail, bracketed paste, and existing keyboard chords. `docs/terminal-robustness.md` captures runtime evidence, CLI versions (`claude` 2.1.197, `codex-cli` 0.141.0), and the manual TUI checklist. Verified with `npm run build`, `npm test` (53 tests), `cargo test` (25 tests), `cargo fmt --check`, `npm run qa:editor`, selected/narrow screenshot inspection, and `git diff --check`.
- ~~**CONTEXT-MENUS:** right-click/Control-click menus across rail, files, editor, terminal, browser, git/diff, and agent panes.~~ **DONE 2026-07-13** — one shared keyboard-accessible renderer now drives project, chat, file, editor, browser, Git, composer, utility-tab, and terminal-pane menus. Async failures surface visibly; disabled and danger states are enforced; Close Project performs the real stop/remove/fallback lifecycle. Utility and pane tabs expose focus/restart/kill/close/hide actions instead of browser-native menus.
- ~~**CHROME-POLISH-SYSTEM:** Codex-level workbench chrome tokens, density, states, badges, menus, and visual QA.~~ **DONE 2026-07-09** — current v0.5 chrome now has a primitive → semantic → component CSS token contract, shared control/menu/badge/field/overlay states, visible focus, disabled/danger/success/warning styling, reduced-motion handling, and docs in `docs/chrome-polish-system.md`. Screenshot QA now includes `chrome-states` alongside selected, dirty, context-menu, recovery, and narrow captures. Icon replacement stays in `ICON-SYSTEM`; browser and diff/review now consume these tokens, while settings and command palette remain future surfaces. Verified with `npm run build`, `npm test` (53 tests), `npm run qa:editor`, visual inspection of `docs/qa/editor-parity/chrome-states.png` and `docs/qa/editor-parity/narrow.png`, and `git diff --check`.
- ~~**ICON-SYSTEM:** polished Codex-quality iconography, labels, tooltips, and status badges.~~ **DONE 2026-07-09** — current v0.5 chrome now uses `lucide-react` through `app/src/icons.tsx` as one SVG icon contract. File-tree object icons, rail actions, editor actions, terminal status pills, composer controls, browser preview, source-control/diff entry points, context-menu rows, and QA fixtures use `currentColor` icons on a 16px dense grid. Non-universal controls keep labels, tooltips, or accessible names. Pane status and future activity status mappings cover thinking/running/waiting/error/exited/complete. Source-host and multi-pane-specific icons remain deferred until those real surfaces land. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.
- ~~**ACCESSIBILITY-BASICS:** keyboard reachability, visible focus, and labelled chrome controls.~~ **DONE 2026-07-09** — current v0.5 chrome has explicit labels for rail actions, workspace root, agent profile picker, terminal canvas, composer draft, browser preview controls, and diff review controls; editor tabs put tab semantics on the actual focusable buttons; the terminal canvas is tabbable and has a visible focus ring; context menus focus the first enabled item, support Arrow Up/Down, and close with Escape. Full modal trapping, canvas transcript screen-reader review, and deeper settings/multi-pane accessibility rules remain deferred. Verified with `npm run build`, `npm test` (57 tests), `npm run qa:editor`, screenshot inspection, and `git diff --check`.

## v1 — Multi-project, multi-agent cockpit

- ~~**PROJECT-RAIL:** Codex-style left project/workspace rail replaces separate VS Code windows.~~ **DONE 2026-07-09** — the left rail now has persisted `openProjects` separate from recents, shows at least three project rows in QA, marks active/running/exited/attention states, promotes opened folders into the rail, prunes missing paths, and exposes switch/reveal/copy/close context-menu actions. Switching projects uses the real workspace load path and restores in-session editor tabs, active file, editor buffers, and view state. Project session rows, browser preview URL state, and multiple live panes are now implemented; richer lifecycle status and cross-relaunch session restore stay with their dedicated later cards. Verified with `npm run build`, `npm test` (59 tests), `npm run qa:editor`, screenshot inspection, and `git diff --check`.
- ~~**PROJECT-SESSIONS:** saved task/workbench context rows.~~ **DONE 2026-07-09; PRODUCT MODEL SUPERSEDED 2026-07-13** — the persistence and context restore remain, but rows are now real chats with independent messages/provider identity under DIRECT-AGENT-HARNESS. `ProjectSession` is an internal compatibility name.
- ~~**BROWSER-PREVIEW:** lightweight browser/web preview pane for localhost apps, docs, auth flows, and generated pages.~~ **DONE 2026-07-09** — the workbench now has a docked iframe preview pane between editor and terminal, with Back, Forward, Reload, address/local URL input, Open, and Open externally controls. Preview URLs are normalized, remembered per project and active project session through `browserPreviewByProject` and `browserPreviewBySession`, restored on project/session switch, and exposed through a browser context menu with copy/open/navigation actions. Dev-server auto-detection and agent-controlled browser actions stay with their dedicated later cards. Verified with `npm run build`, `npm test` (68 tests), `npm run qa:editor`, screenshot inspection, and `git diff --check`.
- ~~**PANE-MANAGER:** multiple Claude/Codex/shell panes per project.~~ **DONE 2026-07-09** — backend `PtyState` now owns multiple real pty/process panes keyed by pane id, `grid` events include `paneId`, and keyboard/paste/scroll/resize route to the focused pane. `open_workspace` no longer kills panes from other projects; project switches reuse existing pane sets where available. The terminal header has a pane strip, New pane profile selector, New, and Close controls; `close_pane` kills exactly one selected process with confirmation and focuses a remaining pane. `docs/pane-manager.md` records boundaries for pane names, restart/kill-all, layout restore, and app-owned handles. Verified with `npm run build`, `npm test` (70 tests), `cargo test` (26 tests including pane focus/close state), `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.
- ~~**PANE-NAMES:** pane names and task labels.~~ **DONE 2026-07-09** — terminal panes now have editable task labels shown in the pane strip and composer target. Labels normalize input, fall back to profile/index names when blank, persist in `workspace.json` by project-session plus pane slot, and can be edited by double-clicking the pane pill or using the terminal context menu. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.
- ~~**AGENT-SESSION-HANDLE:** app-owned handle contract for every pane/session: send, interrupt, readTail, close, cwd, profile, state, approval mode, and activity metadata.~~ **DONE 2026-07-09** — `app/src/agentSessionHandle.ts` defines stable pane-backed handles with project/session ids, cwd, pane label, profile id/label, process state, approval mode, exit code, created time, activity metadata, and `send`/`interrupt`/`readTail`/`close` operations. The composer now sends prompts through the active handle; Stop and Close route through handle methods; terminal context menu can copy the latest 20 rendered terminal lines through `readTail`. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, and `git diff --check`.
- ~~**AGENT-ACTIVITY:** visible thinking/running/tool/waiting/error/exited/complete state. Show Codex-style activity rows and tool events, not hidden chain-of-thought.~~ **DONE 2026-07-09** — `app/src/agentActivity.ts` now normalizes in-memory activity rows scoped by project/session/pane. The selected pane shows a current status row from its handle plus recent rows for prompt sent, app command ran/failed, edited file, created pane, stop sent, copied output, pane closed, complete, and command failed on non-zero exit. Rows render above the composer with shared icons/status colors and accessible labels; the QA fixture covers running, prompt, edited file, ran command, and failed command rows. Approval rows and durable/filterable history are covered by APP-ACTIONS-MINIMAL and AGENT-ACTIVITY-LOG. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.
- ~~**AGENT-ACTIVITY-LOG:** per-pane/session event timeline for thinking summaries, file edits, commands, tool/app events, git/source-control events, approvals, errors, and completion.~~ **DONE 2026-07-09** — `agentActivityEvents` now persists up to 200 user-safe events with project/session/pane attribution, kind, label, detail, status, timestamp, target, exit-code, output-reference, and undo-hint fields. The selected pane shows a durable filterable timeline below the activity strip; current producers cover prompts, app commands, app-action approvals including Open Diff, file saves, pane lifecycle actions, copied output, and process exits. Git mutation, richer browser, and tool producers use the reserved event model when those real surfaces land. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.
- ~~**APP-ACTIONS-MINIMAL:** minimal gated app-owned actions for focus/open/interrupt/create/attach/open-diff before permission controls imply more than they enforce.~~ **DONE 2026-07-09** — `app/src/appActions.ts` defines action descriptors with risk class, requester, decision, audit, and undo metadata. Focus pane, open file, open diff, open browser preview, attach reference, interrupt process, create pane, close pane, and composer app commands route through the gate; prompted/denied/blocked/composer decisions log approval audit rows to the timeline. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.
- ~~**COMPOSER-HARNESS:** attachments, permission, goal, model/reasoning, context, and stop/send controls for each structured chat.~~ **CORRECTED 2026-07-13** — persisted per-chat state drives real attachment, Ask/Approve/Full access, Goal, Codex model override, and reasoning menus. Model/reasoning reach both new and resumed Codex CLI runs; active runs replace Send with Stop. Deterministic and packaged-native menu captures verify the controls open without clipping.
- ~~**PROCESS-LIFECYCLE:** running/exited status, restart, kill, command/cwd visibility.~~ **DONE 2026-07-09** — backend `restart_pane` replaces a selected pane with a fresh process while preserving the frontend slot/label, and `terminate_pane` kills a selected process without removing the pane/transcript. The terminal header exposes command, mode, cwd, running/exited state, Restart, Kill, New, and Close; narrow widths keep controls as icon buttons without overlap. Restart/terminate route through the action gate and activity timeline. Verified with `npm run build`, `npm test`, `cargo test`, `cargo fmt --check`, `npm run qa:editor`, screenshot inspection, and `git diff --check`.
- ~~**AGENT-FIRST-WORKBENCH:** movable/resizable agent-first tray hierarchy.~~ **DONE 2026-07-10; CENTER MODEL SUPERSEDED 2026-07-13** — the tray/docking work remains; structured chat now owns the center and Raw terminal is optional.
- ~~**VSCODE-CHROME-SHELL:** outer chrome adapts the VS Code/Codex shell pattern to Keelhouse.~~ **DONE 2026-07-09** — the app now has a compact top status/action bar, persistent side drawer, main agent workbench, and bottom status strip. The header shows real Keelhouse state and actions: workspace, session, selected agent/pane, process status, Open Folder, and Reload Preview. Copied fake traffic lights/back-forward/search chrome and the nonfunctional activity rail were removed. Screenshot QA fixture was updated so selected/narrow states prove the titlebar/sidebar/main/status geometry instead of only stacked internal panes. Verified with `npm run build`, `npm test`, `npm run qa:editor`, selected/narrow screenshot inspection, and `git diff --check`.
- ~~**DRAWER-MODES-PROJECTS-FILES:** split the side drawer into real Projects and Files modes.~~ **DONE 2026-07-09** — the left drawer now has a compact Projects/Files tab control. Projects shows only open projects and task sessions; Files shows workspace root, file actions, and the virtualized tree. The file-tree height observer mounts when the Files drawer is shown, so switching modes keeps the tree usable. Screenshot QA fixture defaults to Projects mode. Verified with `npm run build`, `npm test`, `npm run qa:editor`, selected screenshot inspection, and `git diff --check`.
- ~~**ACTIVITY-DRAWERS:** switchable side drawer modes backed by real behavior.~~ **DONE 2026-07-09** — the side drawer now has Projects, Files, Search, Git, Browser, and Settings modes. Search filters the live file tree and opens editor matches; Git reads real `git status --short --branch`; Browser controls the preview URL/history/reload/open-external actions; Settings changes current profile, permission mode, agent surface, tray position, open folder, and file refresh. Drawer width is pointer/keyboard resizable, collapsed state is icon-only, and width/collapse persist locally. Verified with `npm run build`, `npm test`, `cargo test`, `npm run qa:editor`, selected screenshot inspection, and `git diff --check`.
- ~~**SESSION-RESTORE:** restore project rail, editor tabs, rail state, pane layout.~~ **DONE 2026-07-09** — `sessionEditorSnapshots` persists per-session editor tabs, active file, dirty buffers, and view state; `paneLayoutsBySession` persists pane slots, profile ids, and labels; startup normalizes both before reopening the last workspace and creates fresh panes from the saved layout. `docs/session-restore.md` records the process/transcript boundary. Verified with `npm run build` and `npm test -- sessionRestore.test.ts`.
- ~~**GIT-STATUS:** dirty/new/deleted markers in the file rail.~~ **DONE 2026-07-09** — file nodes now show compact Git tokens for modified, untracked/new, added, deleted, renamed, and staged files. Deleted files are inserted as virtual rows when they no longer exist on disk, while unsaved editor draft dots remain separate from Git status. Verified with `npm run build`, `npm test -- fileGitStatus.test.ts`, and `npm run qa:editor`.
- ~~**DIFF-VIEW:** inspect agent-created changes without VS Code.~~ **DONE 2026-07-09** — Source Control rows and file-rail Git context menus now open a read-only unified diff review in the editor tray. The review shows source/status, additions/deletions, old/new line numbers, metadata, synthetic untracked text diffs, and hunk-to-editor jumps for files that still exist. Verified with `npm run build`, `npm test -- diffView.test.ts`, `cargo test git_`, and `cargo test synthesizes_untracked_text_diff`.
- ~~**GIT-ACTIONS-LITE:** stage/unstage/discard/copy diff; v1 must-have after DIFF-VIEW, not a full git client.~~ **DONE 2026-07-09** — diff review and Git-marked file context menus now expose Stage, Unstage, Discard Unstaged Changes, and Copy shown diff. The `$blind` audit findings were folded into the card: discard is blocked when the same file has an unsaved editor draft; mixed staged/unstaged files use explicit labels (`Copy shown diff`, whole-file Stage/Unstage); backend `git_file_action` validates repository-relative paths and re-reads target status before mutation; staged new files, tracked discard, and untracked discard use separate Git command paths; real temp-repo Git tests cover stage/unstage/discard; the slice stops at per-file actions, not branch/commit/push/PR; screenshot QA includes `diff-review.png`. Verified with `npm run build`, `npm test`, `cargo test git_file_actions`, `npm run qa:editor`, and `git diff --check`.
- ~~**CHROME-DEMO-CONTRACT:** treat the accepted `demo/keelhouse-chrome-demo.html` as the visual contract for the app pass: graphite shell, steel-cyan accent, direct thread/project sidebar, agent-first center, right/bottom trays, flat toolbar icons, and no orange accents, fake browser chrome, decorative activity rail, or pill-heavy controls.~~ **DONE 2026-07-09** — the contract is documented in `docs/chrome-contract.md`, enforced by `npm run qa:chrome-contract`, and tied to the accepted demo plus real app shell checks.
- ~~**CHROME-QA-GATE:** every chrome pass must prove the actual shell, not only a fixture.~~ **DONE 2026-07-10** — `npm run qa:shell` captures the real React app at 1440/1024/900; `npm run qa:chrome-contract` requires those artifacts and the Run/tray/control contract. The editor fixture remains for deterministic menu, diff, modal, and failure states. Native proof lives at `docs/qa/app-shell/native-run.png`.
- ~~**STEEL-CYAN-THEME-GATE:** lock the current graphite + steel-cyan palette into semantic tokens and QA checks; reject orange/warm accent drift, purple-heavy gradients, and one-note palettes.~~ **DONE 2026-07-09** — app chrome now defines `--steel-cyan-400`, `--steel-cyan-500`, and `--steel-cyan-900`, routes semantic accent tokens through them, removes the old blue primary button treatment, flattens shared badge/control radii, and the chrome contract gate rejects warm accent drift.
- ~~**AGENT-THREAD-SURFACE:** make readable agent output the default working surface without fake chat identity or hidden reasoning.~~ **SUPERSEDED 2026-07-13** — the terminal-backed pseudo-chat exposed the wrong product object in native QA. DIRECT-AGENT-HARNESS now makes structured Codex chats primary; Raw terminal remains the exact TUI path.
- ~~**APP-CHROME-PORT:** port the accepted direction into the real Tauri app shell without regressing existing v1 behavior.~~ **DONE 2026-07-10; CENTER MODEL SUPERSEDED 2026-07-13** — native shell and optional-tool integration remain proven; the terminal-backed Run plus separate Activity center was replaced by continuous structured chat and composer. Historical screenshots are implementation evidence, not the current visual contract.
- ~~**CHROME-CONTROL-POLISH:** flatten obvious AI-looking control clusters into native-feeling toolbar/menu/dropdown controls.~~ **DONE 2026-07-10** — redundant activity chips and inline tray/dock buttons were removed; drawer modes use icon tabs; run settings and tool position use compact labelled popovers; shared outline icons and interaction states remain consistent.
- ~~**TRAY-DOCKING-UX:** make editor and browser optional movable/resizable tools rather than permanent stacked panels.~~ **DONE 2026-07-10** — first open hides tools; Editor is the first-open tool; Editor/Browser/Split and left/right/bottom/hidden states live in one menu; responsive side docks become bottom docks at narrow widths; layout, sizing, and hidden-tray behavior are tested.
- ~~**CHROME-COHERENCE-PASS:** final integration gate for a clean agent-first shell.~~ **DONE 2026-07-10** — the real first-open shell is coherent at 1440, 1024, and 900px; the native window proves live terminal output; deterministic fixtures retain menu/settings/palette/error coverage without being treated as app-shell proof.

### Chrome re-convergence (added 2026-07-11 — executes before the remaining v1 queue)

The accepted `demo/keelhouse-chrome-demo.html` is now the shell contract as well as the control-grammar reference. The packaged app uses Threads left, persistent structured chat/composer center, Files/Editor/Browser/Git right, and Terminal/Processes/Logs in a collapsible bottom tray. Browser is not duplicated below. Evidence: `docs/qa/chrome-v3/` and `docs/qa/interaction-contract/`.

- **CHROME-CONTROL-GRAMMAR** (blocker): replace boxed default-button chrome with the demo's three-control grammar — flat 600-weight text actions with accent-strong hover, transparent icon buttons with color-only hover, and a single filled Send. Danger actions become flat danger text-actions; confirmations keep their modals; hit areas stay ≥40px.
- ~~**RUN-SURFACE-COMPOSITION:** compose one continuous agent thread with inline provenance and an anchored composer.~~ **NATIVE VERIFIED 2026-07-13** — structured provider chat remains mounted while raw terminal and other utilities open in the bottom tray.
- **COMPOSER-ELEVATION:** keep the composer on the transcript's centered `56rem` axis — 8px radius, hairline border, restrained shadow, gradient fade wrap, borderless textarea, compact functional menu bar, and one circular Send position that becomes Stop during an active run. The observed Codex 48rem variant was rejected as a direct product token after populated native review; the shared-axis relationship remains source-grounded while Keelhouse's width is evidence-tuned.
- **SIDEBAR-RHYTHM:** 11px/600 uppercase section labels, 34px rows, 600-weight project rows with 7px status dots, 28px-indented chat rows with real recency stamps, and background-only selection. Decorative left stripes are rejected.
- **TITLEBAR-STATUSBAR-PARITY:** calm 44px primary/thread toolbar rhythm with workspace crumb, flat panel toggles, and agent state; duplicate search/folder actions are removed. The three-section statusbar remains but is subordinate at 20px.
- ~~**TRAY-TAB-CHROME:** Files/Editor/Browser/Git share the right dock; Terminal/Processes/Logs share the resizable bottom tray.~~ **NATIVE VERIFIED 2026-07-13** — Browser Preview duplication was removed; utility and pane tab menus were executed in the packaged app; active pane tabs use a flat underline rather than rounded capsules; raw Terminal opens `/bin/zsh -l` instead of an agent.
- ~~**FIRST-OPEN-LAYOUT:** first open shows Threads left, conversation/composer center, Files right, collapsed utilities, and one overlay titlebar.~~ **NATIVE VERIFIED 2026-07-13** — versioned preferences plus Reset restore the approved geometry.
- **OVERLAY-PARITY:** compact context menus and command-palette rows use background-only active states; settings navigation follows the same rule. Composer approval, Goal, and runtime menus use the shared overlay depth and spacing grammar.
- **CHROME-CONTRACT-V2:** current gate rejects boxed/side-highlight drift, duplicate Browser surfaces, rounded pane tabs, and missing tab menus. The packaged shell now also uses global-only titlebar controls, inset terminal actions, and real-only status text. Formal lock still executes after CHROME-EYEBALL-SIGNOFF.

### Blind-audit additions (2026-07-11 — see `docs/blind-audit-chrome-roadmap-2026-07-11.md`)

A 16-framework blind-spot audit of the chrome re-convergence and roadmap coverage produced these cards. v1 additions close proof gaps before daily-driver claims; v2 additions are reference-class categories the roadmap had no row for.

- ~~**DIRECT-AGENT-HARNESS** (v1, rock, promoted 2026-07-13): Codex-style independent chats backed by structured `codex exec --json` start/resume/stop events and existing OAuth.~~ **DONE 2026-07-13** — packaged native QA executed two independent same-project chats, provider-thread resume, concurrent runs, selected-run process-group cancellation, raw-terminal alternation, and quit/relaunch restoration. Evidence: `docs/qa/daily-driver/codex-multi-chat.md`.
- ~~**CHAT-DURABLE-STORE** (v1, rock): adapt Hashmark's SQLite WAL session/message model so chat history, provider thread ids, structured blocks, usage, and migrations do not live in a capped `workspace.json` array.~~ **DONE 2026-07-13** — Rust-owned SQLite WAL storage, transactional revision-ordered saves, one-time legacy migration, recovery, and packaged Codex quit/relaunch proof. Evidence: `docs/qa/daily-driver/chat-durable-store.md`.
- ~~**CHAT-RICH-MESSAGES** (v1, rock): safe Markdown/GFM, highlighted code copy, compact tool rows, retry, elapsed state, scroll anchoring, Jump to latest, accessible live status, and provider-native app-server deltas.~~ **DONE 2026-07-13** — the current package rendered the preserved rich Markdown/code turn, executed Copy code, and exposed the real stopped-run transcript; automated tests cover tables, sanitization, streaming, retry, tool rows, anchoring, elapsed state, and live status. Evidence: `docs/qa/chat-rich-messages/README.md`.
- ~~**CHAT-PROVIDER-APPROVALS** (v1, rock): run-scoped app-server command/file/permission requests pause the requesting chat with exact action/cwd/target/reason, durable attribution, timeout/close resolution, and Allow once, Allow for session, or Deny controls.~~ **DONE 2026-07-13** — packaged native QA executed allow, deny, stop, concurrent-chat completion, and relaunch restoration; Rust tests cover timeout/run isolation; direct provider allow/cancel smokes passed. Evidence: `docs/qa/chat-provider-approvals/README.md`.
- **CHAT-COMPOSER-CONTEXT** (v1): per-chat drafts/history, `@file`, bounded file payloads, image paste/drop/picker cache, removable chips, exact context review, provider-native image inputs, and unsafe-attachment preflight are implemented. Packaged draft isolation passes; one native file send and image send remain before Done.
- ~~**CHAT-HISTORY-DISCOVERY** (v1): SQLite-backed message search, pinning, bookmarks, archive compatibility, and message-result navigation.~~ **DONE 2026-07-13** — packaged QA exercised cross-project message search, exact-result navigation, bookmark filtering, pin/archive behavior, and relaunch persistence. Evidence: `docs/chat-history-discovery.md`.
- **RUN-CONTENT-DENSITY-QA** (v1): populated-state visual proof — live PTY output in the run card (incl. the native tauri smoke), stacked activity rows, three-project sidebar, multi-pane, plus a ≥600px run-column legibility floor. All current proof is empty-state.
- **DAILY-DRIVER-LIVE** (v1, rock): timed live runs of the three replacement workflows with thresholds, VS Code memory/CPU comparison, and a Gemini TUI render smoke. The structured two-chat workflow is now executed; one-project edit+agent+preview, complete three-project relaunch timing, Gemini prompt/response, and controlled VS Code comparison remain. DAILY-DRIVER-METRICS is readiness-only and must not be cited as North-Star proof.
- ~~**SESSION-PANE-ISOLATION** (v1, rock): key optional raw-terminal panes and active-pane state by project + chat so same-project chats never share or relabel processes.~~ **DONE 2026-07-13** — packaged QA ran independent Alpha/Beta shells with distinct process ids and output, switched without restart, and relaunched into fresh per-chat processes with restored labels/layouts. Evidence: `docs/qa/daily-driver/session-pane-isolation.md`.
- **A11Y-CHROME-AUDIT** (v1): computed WCAG ratios for the 2026-07-11 token retargets (never measured), pointer target-size verification, reduced-motion sweep (one guard block today), visible labels/tooltips for the icon-only drawer mode switcher.
- **CHROME-EYEBALL-SIGNOFF** (v1): corrected packaged evidence now includes collapsed, live Terminal, and Processes states in `docs/qa/chrome-v3/`. Jason's explicit visual verdict is still required before CHROME-CONTRACT-V2 locks.
- **CHROME-FONT-LOCK** (v1): Inter is first in the font stack but not bundled — bundle it or go system-first, then re-baseline screenshots.
- **WORKBENCH-STATE-GUARDS** (v2): pane aggregate-status atomicity, pane-cwd vs editor-project mismatch banner, titlebar project-ownership rule, activity-log overflow policy.
- **STATE-MIGRATION** (v2): schemaVersion + migration path for `workspace.json` before post-adoption schema changes.
- **CRASH-RESILIENCE** (v2): abnormal-exit detection, crash-recovery notice, local health log.
- **TERMINAL-INTL-INPUT** (v2): IME composition, non-Latin input, glyph fallback on the canvas path.
  - 2026-07-12: root cause confirmed and fixed — canvas never receives composition events in WebKit (same class of issue as the existing `Cmd+V` paste workaround); a hidden always-focused textarea (xterm.js's pattern) now holds terminal-pane focus, positioned over the cursor cell each paint tick, `compositionend` sends composed text via the existing paste path. CJK font-fallback chain added. Verified: build/test (159/159)/qa:chrome-contract green, live smoke test confirms no keyboard regression from the canvas→textarea focus change. Enter path also live-confirmed working (`osascript keystroke` after `cliclick`'s Return was isolated as broken on this machine, unrelated to the app). NOT live-verified: real CJK IME round-trip, Option-dead-key composition (one attempt was inconclusive — window lost frontmost mid-sequence, not a negative result). Full writeup: `docs/terminal-robustness.md`. Stays `next` pending Jason's live confirmation of the two remaining items.
- **WINDOW-LIFECYCLE** (v2): window frame restore, off-screen recovery, mixed-DPI canvas checks.
- **MOTION-POLISH** (v2): the micro-motion criteria from `docs/chrome-ui-polish.md`, reduced-motion-guarded.
- **APP-COMMANDS-DISCOVERABLE** (v2): `>help`, palette rows for `>` commands.
- **OUTSIDE-REVIEW** (v2): one targeted external pass on chrome quality / agent workflow before public daily-driver claims.
- **CHAT-FORK-CHECKPOINT** (v2): adapt Hashmark's chat lineage and restore UX, but replace its `git add -A` / `reset --hard` / `clean -fd` checkpoint implementation with non-index-mutating capture and previewed restore.
- **CLAUDE-STRUCTURED-ADAPTER** (v2): adapt Hashmark's Claude `stream-json` event parser behind Keelhouse's provider-neutral contract; preserve OAuth and approvals, and reject `--dangerously-skip-permissions` plus prompt-in-argv behavior.
- **AGENT-ORCHESTRATION** (v2): parent/child chat lineage, previewed parallel dispatch, worktree isolation, resource limits, selective cancellation, and attributed result handoff based on Hashmark's spawn/dispatch concepts.
- **UNINSTALL-RESET** (v3): on-disk artifact inventory, settings full-reset, uninstall cleanup docs.

Amended by the same audit: FIRST-OPEN-LAYOUT (spawn-failure recovery banner, reset-to-demo, first-open-matches-demo check), RUN-CARDS-ADAPTER (mechanical provenance gate against terminal-text inference), PERF-BUDGET (render-perf gate: IPC payload, frame time at 1/2/4 panes, jank threshold), DAILY-DRIVER-METRICS (readiness-only wording).

- **DEV-SERVER-DETECT:** detect common localhost dev servers and offer to open them in the browser preview.
  - 2026-07-09 slice: terminal output is scanned for local dev-server URLs (`localhost`, `127.0.0.1`, `0.0.0.0`, `::1` with ports); detections are scoped to the emitting project session, logged as browser activity, and offered as explicit Open detected actions in the Browser drawer and preview pane.
- **DAILY-DRIVER-METRICS:** prove the app can replace the current workflow with scripted one-project edit+agent, two-agent same-project, and three-project switch/relaunch runs.
  - 2026-07-09 slice: added `npm run qa:daily-driver`, which writes `docs/qa/daily-driver/latest.json` and `.md` with readiness evidence for the three replacement workflows. This proves the required app surfaces/evidence exist; timed live Tauri runs and VS Code comparison remain next.
- **PERF-BUDGET:** prove this is lighter than the VS Code workflow it replaces across 1-project, 2-agent, and 3-project task runs.
  - 2026-07-09 slice: added `npm run qa:perf-budget`, which writes `docs/qa/perf-budget/latest.json` and `.md` with hard static budgets for built JS/CSS, required screenshot/evidence checks, lightweight source-architecture checks, and explicit next live measurements. This creates the baseline gate; packaged Tauri memory/CPU/jank and VS Code comparison remain next.
  - 2026-07-12 slice: built and live-verified the render-perf gate (`renderPerf.ts` — unit-tested frame-time/IPC-payload rolling stats, wired into the paint loop and grid listener; a command-palette action snapshots real numbers). A one-pane dev burst recorded 3.8ms average/53ms p95, 6.25% jank. Packaged two-pane output recorded 1.66ms average/~1ms p95 and 1.71% jank; packaged four-pane overlapping output recorded ~0.54ms average/1ms p95, 0% jank, and 220 IPC samples averaging ~108KB. `npm run qa:runtime-perf` adds repeatable process-tree samples, but the current VS Code comparison is observational rather than controlled equivalent proof. Inter Latin subsets reduced CSS to 87,989 bytes, under the 90KB cap.

## v2 — Workflow leverage

- **COMMAND-PALETTE:** VS Code-style compact action access without adding IDE chrome; uses the same command registry as shortcuts and context menus.
  - 2026-07-09 slice: added a real `Shift+Cmd+P` command palette plus titlebar Commands entry. It filters command labels/details/shortcuts/keywords, runs existing app-owned actions for open/save/find/tabs/panes/browser/drawers/trays/composer attachments, shows shortcut labels where assigned, and keeps `Cmd+P` quick-open/search deferred to `SEARCH`.
- **SEARCH:** file quick-open and ripgrep-backed text search.
  - 2026-07-09 slice: added real `Cmd+P` Quick Open for workspace files and upgraded the Search drawer to Files/Text scopes. Text search calls `search_workspace_text`, which uses `rg --json --fixed-strings --ignore-case` when available and falls back to the existing ignore-aware walker. Results show file, line, column, and snippet, then open the file and jump to the matching line.
- **TERMINAL-FIND:** search active terminal output/scrollback.
- ~~**SETTINGS-PARITY**~~ **DONE 2026-07-13:** the cramped modal is replaced by a dedicated searchable workspace with grouped navigation, Back/Escape return, responsive category fallback, and truthful Global/Project/Chat ownership. Provider health and custom raw-terminal profiles are real; Commands/Files/Open tabs/Worktrees palette sources persist and filter live objects; worktree, lifecycle-hook, and environment policy rows state current and unavailable behavior without fake controls. Packaged QA covers inheritance reset, provider/profile management, compact navigation, source off/on behavior, real README discovery, policy rows, and return to workbench. See `docs/settings-parity.md` and `docs/qa/settings-workspace/`.
- **SETTINGS:** inspectable config for agent commands, ignored folders, font/theme, layout, AI connections, and shortcut overrides.
- **KEYBINDINGS-CONFIG:** configurable app shortcut overrides after defaults stabilize, including conflict detection and a visible shortcut reference.
- **THEME:** color themes across chrome, terminal, rail, and editor; start with mono-ghost and preserve the chrome token contract.
- **AI-CONNECTIONS:** settings for providers, API keys, MCP servers, CLI auth, env vars, model defaults, and permission policy.
  - 2026-07-14 foundations: added typed provider/MCP/project-environment records, provider model defaults, native macOS Keychain storage for provider keys/bearer tokens/secret environment values, write-only secret IPC, reset cleanup, and stdio/HTTP target validation. New structured Codex runs and all raw-terminal launch/restart paths now resolve project and provider secrets only in Rust at process spawn. Remaining: live MCP discovery, OAuth PKCE/token lifecycle, and packaged credential validation. See `docs/ai-connections.md`.
- **AGENT-HOOKS:** built-in MCP/API surface so agents can inspect app state and request app-owned actions.
- **TRANSCRIPTS:** save/review completed pane output.
- **NOTIFICATIONS:** background agent exit/attention badges and optional macOS notifications.
- ~~**WORKTREE:** create disposable worktree + agent pane from a project.~~ **DONE 2026-07-12** — `New Worktree Pane`/`Remove Worktree` in the terminal context menu drive Rust `create_project_worktree`/`remove_project_worktree` (`git worktree add -b`/`remove --force` + `branch -D`), persisted via `worktrees.ts` store helpers. Verified: `cargo test` 39/39 (incl. a real-repo add/remove round-trip), `npm test` 152/152, `npm run build`, `cargo fmt --check`, `qa:chrome-contract` all green. Gap: no live `npm run tauri dev` UI smoke test yet — build-verified, not native-UI-verified.
- **INTEGRATIONS-POLICY:** define which other integrations are allowed, parked, or explicitly out of scope.
- **SOURCE-CONTROL-CONNECTIONS:** GitHub/GitLab/source-host auth, repo links, PR/MR status, CI/pipeline status, and health checks.
  - 2026-07-12 detection slice: `source_control_status` (Rust) detects git/gh/glab presence and auth health read-only, no token material ever returned/stored/logged; Settings > Git shows a live status row. Verified against real machine state (git present, gh authenticated, glab absent) via `cargo test`, `cargo fmt --check`, `npm run build`, `npm test`, `qa:chrome-contract`.
  - 2026-07-12 links slice: `git_remote_url` (Rust) + `sourceControlLinks.ts` (SSH/HTTPS parsing, GitHub/GitLab/self-hosted host detection, link building) power a Settings > Git > Remote row (Repo/PRs/Issues/Actions-or-Pipelines, opened via `@tauri-apps/plugin-opener`). Verified: `cargo test` 43/43, `npm test` 165/165, cross-checked against this repo's real origin remote. Remaining: active-repo host status outside Settings, OS-safe credential storage.
- ~~**PACKAGING:** local macOS `.app` packaging.~~ **DONE 2026-07-12** — `npm run package:mac` creates an arm64, ad-hoc-signed `Keelhouse.app` using a scoped ignored Zig-lib compatibility copy. The packaged app restored the real workspace, spawned a shell, and launched Codex. See `docs/packaging.md`.
- **SESSION-ARCHIVE:** archive old project chats without losing structured messages, provider thread ids, raw-terminal transcripts, or workspace context.
- **RUN-CARDS-ADAPTER:** the demo's structured thinking/working-plan/edited-file-with-Review/approval cards, fed by real agent events through AGENT-HOOKS adapters — never inferred from terminal text. Reuses the card components shipped in RUN-SURFACE-COMPOSITION; without hooks, Run stays terminal-backed with no fabricated structure.

## v3 — Polish and shipping

- **REUSE-AUDIT:** mine Hallmark/hashmark/brutal/indx for reusable Tauri shell, persistence, editor, or design patterns without inheriting the wrong product shape.

## Research-backed stack per phase (2026-07-08)

Deep-research (6 angles, 26 sources, 24/25 claims verified) locked a concrete library to each phase. Full report + citations: `docs/vision-to-reality-2026-07-08.html`. DECISIONS.md 2026-07-08.

- **v0** — `portable-pty` (have it) + **Tauri Store plugin** for last-folder.
- **v0.5** — rail: `ignore::WalkBuilder` (gitignore-aware) + `notify`+`notify-debouncer-mini` (FSEvents, debounce or storm) + **React Arborist** (virtualized VS-Code-style tree). editor: **`@uiw/react-codemirror`** (CM6; ~300KB vs Monaco ~5–10MB; Monaco doesn't support webviews).
- **v1** — **Tauri SQL/SQLite plugin** when state turns relational; `Child`/`ChildKiller`/`ExitStatus` lifecycle; **measure the process ceiling** (4–8+ ptys) here. Terminal under load: **dirty-region IPC deltas first**, WebGL renderer only if measured IPC-bound (WebKit WebGL2 works — the "no WebGL2" blocker was refuted).
- **v3** — Tauri CLI `.app` bundle, **ad-hoc sign** for local use; avoid `externalBin` sidecars (break notarization).
